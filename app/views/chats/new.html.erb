<% if user_signed_in? %>
  <h3>Qu'elle langage veut tu apprendre ?</h3>

  <div class="d-flex justify-content-center gap-3 mt-4">
    <%= link_to "Login", new_user_session_path, class: "nav-link" %>
  </div>
<% end %>
