<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-8 text-center">

      <!-- Titre principal -->
      <h1 class="display-4 fw-bold mb-4">
        Bienvenue sur <span class="text-primary">Deva</span>
      </h1>

      <!-- Proposition de valeur -->
      <p class="lead mb-4">
        La plateforme qui simplifie vos révisions<br>
        Générez des exercices, testez vos connaissances.
      </p>

      <% if user_signed_in? %>
        <%= link_to "Start", new_chat_path, class: " btn btn-primary"  %>
      <% end %>

    </div>
  </div>
</div>
